experiment:
  name: synthetic-oversight-sensitivity-v0
  seed: 7
  n_samples: 2000
  n_features: 8
  effect_size: 0.8

split:
  test_size: 0.30
  stratified: true

metrics:
  - baseline_rate
  - oversight_effect
  - probe_auroc
  - probe_auprc

safety:
  environment: synthetic_only
  external_actions: false
  real-world-targeting: false
